/** @type {import('next').NextConfig} */

const nextConfig = {
    images: {
        domains: ['imgs.ysscores.com', 'lh3.googleusercontent.com', 'i3.ytimg.com', 'www.ysscores.com']
    },
};

export default nextConfig;
